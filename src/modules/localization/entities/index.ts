import { LocalizationSettingsEntity } from './localization-settings.entity';

export const localizationEntities = [LocalizationSettingsEntity] as const;

export {
  LOCALIZATION_SETTINGS_KEY,
  LocalizationSettingsEntity,
} from './localization-settings.entity';
